import {
  REQUEST_CATEGORIES,
  RECEIVE_CATEGORIES,
  TOGGLE_CATEGORY
} from '../actions'

const user = (state = {}, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default user
